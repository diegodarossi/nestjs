export class Domain {

    // Domain for smaller descriptions with a maximum of 20 positions
    static descriptionMin: number = 20;

    // Domain for smaller descriptions with a maximum of 40 positions
    static descriptionAvg: number = 40;

    // Domain for smaller descriptions with a maximum of 60 positions
    static descriptionMax: number = 60;
}